/*DOC Returns true if op would fit in the int when rounded to an integer using RoundMode. */
RTEST(fitsSInt, fits_sint_p)

/*DOC Returns true if op would fit in the long when rounded to an integer using RoundMode. */
RTEST(fitsSLong, fits_slong_p)

/*DOC Returns true if op would fit in the short when rounded to an integer using RoundMode. */
RTEST(fitsSShort, fits_sshort_p)

/*DOC Returns true if op would fit in the unsigned int when rounded to an integer using RoundMode. */
RTEST(fitsUInt, fits_uint_p)

/*DOC Returns true if op would fit in the unsigned long when rounded to an integer using RoundMode. */
RTEST(fitsULong, fits_ulong_p)

/*DOC Returns true if op would fit in the unsigned short when rounded to an integer using RoundMode. */
RTEST(fitsUShort, fits_ushort_p)
