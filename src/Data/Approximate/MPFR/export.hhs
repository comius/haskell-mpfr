#define UNARY_(hname,cname) hname,
#define UNARY(name) name,
#define BINARY(name) name,
#define CONST(hname,cname) hname,
#define UNARY2(hname,cname) hname,
#define TERNARY(name) name,
#define COMPARISON(hname, cname) hname,
#define ROUNDING(name) name,
#define TEST(hname, cname) hname,
#define RTEST(hname, cname) hname,
